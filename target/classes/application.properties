spring.application.name=Project
spring.datasource.url=jdbc:mysql://localhost:3306/project_db?createDatabaseIfNotExist=true&serverTimezone=Asia/Manila
spring.datasource.username=root
spring.datasource.password=Senfai_123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# dev: auto create / update schema
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=8080
# ensure Spring runs data.sql on startup even for MySQL (non-embedded)
spring.sql.init.mode=always
spring.sql.init.platform=mysql
# optional: explicit location if needed
# spring.sql.init.data-locations=classpath:data.sql


# SMTP (Gmail example) — replace with your values
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nestymarthegreat@gmail.com
spring.mail.password=ndqj kemq xmbb shfx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.debug=true

# OTP expiry (minutes)
app.otp.expiry-minutes=10
